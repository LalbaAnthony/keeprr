# Keeprr

Keeprr is a simple Google Keep clone.

## 🚀 - Quick start

```sh
# Clone the repo
git clone git@github.com:LalbaAnthony/keeprr.git

# Move into the project directory
cd keeprr

# Copy .env.development file to .env and fill in the required environment variables.
cp .env.development .env

# Start the backend
cd backend
npm i ; npm run dev

# Start the frontend
cd frontend
npm i ; npm run dev
```